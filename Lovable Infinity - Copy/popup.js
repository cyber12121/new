// Popup JS — PATCHED: License manager removed, just shows status
document.addEventListener('DOMContentLoaded', () => {
    // Nothing to validate — extension is already active
    console.log('[Lovable Infinity] Popup loaded — patched edition');
});